$kind: http-request
method: POST
url: "{{baseUrl}}/api/v1/packages"
order: 3000
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "name": "Team hangout",
      "description": "Team hangout for the  year after work",
      "roomId": "{{roomId}}",
      "flightNumber": "Airpiece123",
      "origin": "Lagos",
      "destination": "Kaduna",
      "price": 850.00
    }
